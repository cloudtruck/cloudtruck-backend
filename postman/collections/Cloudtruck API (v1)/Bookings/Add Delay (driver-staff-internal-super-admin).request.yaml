$kind: http-request
name: Add Delay (driver/staff/internal/super-admin)
url: "{{baseUrl}}/bookings/{{bookingId}}/delay"
method: POST
headers:
  Accept: application/json
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "reason": "Traffic jam near highway",
      "estimatedDelay": 45
    }
order: 10000
