$kind: http-request
name: Customer Booking History (staff/internal/super-admin)
url: "{{baseUrl}}/customers/{{customerId}}/bookings?page=1&limit=20"
method: GET
headers:
  Accept: application/json
queryParams:
  page: "1"
  limit: "20"
order: 8000
